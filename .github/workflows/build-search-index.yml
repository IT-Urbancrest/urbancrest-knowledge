name: Build Knowledge Search Index

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - AI_PERSONALITY.md
      - STYLE_GUIDE.md
      - manifest.yaml
      - knowledge/**
      - registry/**
      - relationships/**
      - intents/**
      - tests/**
      - scripts/build_search_index.py
      - scripts/build_live_data_freshness.py
      - scripts/finalize_search_index.py
      - scripts/validate_search_index.py
      - scripts/requirements-index.txt

permissions:
  contents: write

concurrency:
  group: urbancrest-knowledge-writes
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: scripts/requirements-index.txt

      - name: Install dependencies
        run: pip install -r scripts/requirements-index.txt

      - name: Check Python syntax
        run: |
          python -m py_compile scripts/build_search_index.py
          python -m py_compile scripts/build_live_data_freshness.py
          python -m py_compile scripts/finalize_search_index.py
          python -m py_compile scripts/validate_search_index.py

      - name: Build search index
        run: python scripts/build_search_index.py

      - name: Build compact live-data freshness metadata
        run: python scripts/build_live_data_freshness.py

      - name: Finalize search index
        run: python scripts/finalize_search_index.py

      - name: Validate search index and fixtures
        run: python scripts/validate_search_index.py

      - name: Commit search index
        run: |
          git config user.name "urbancrest-knowledge-bot"
          git config user.email "actions@users.noreply.github.com"

          git add runtime/search-index.json runtime/live-data-freshness.json

          if git diff --cached --quiet; then
            echo "Search index is already current."
            exit 0
          fi

          git commit -m "chore: rebuild knowledge search index"
          git push
